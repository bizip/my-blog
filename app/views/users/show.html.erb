        <div>
            <div><img src=<%= @user.photo %> alt="profile pic"></li>
            </div>
            <div>
            <h2><%= @user.name%></h1>
                <p>Number of posts: <%= @user.posts_count %></p>
            </div>
        </div>

        <div>
        <h2>Bio<h2>
         <p> <%= @user.bio %></p>
        </div>
           
           <div> <%= @user.recent_three_posts.each do |post|  %>
             <li>
              <b>post #1</b>
                <div>
                    <h2><%= post.title %></h2>
                    <p>Post: <%= post.text %></p>
                </div>
                </li>
            <% end %>
            </div> 
               

        